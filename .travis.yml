language: python
python:
  - "2.7"
# command to install dependencies
install:
  - "python setup.py build"
  - "python setup.py install"
  - "pip install ."
# command to run tests
script: nosetests -v
